
function BooleanInput (labelText, document, callback) {
    var elem = document.createElement ('input');
    elem.setAttribute ('type', 'checkbox');

    // control events
    elem.on ('input', function (event) { console.log ('input'); });
    elem.on ('change', function (event) { console.log ('input'); });

    // wrap the input in a label and return
    var label = document.createElement ('label');
    label.appendChild (document.createTextNode (labelText));
    label.appendChild (elem);
    this.elem = label;
}

BooleanInput.prototype.set = function (value) {

};

BooleanInput.prototype.enable = function (isEnabled) {

};

module.exports = BooleanInput;
