
function ColorInput (labelText, document, config, callback) {
    var triggerBox = document.createElement ('div');

    // control events

    // wrap the input in a label and return
    var label = document.createElement ('label');
    label.appendChild (document.createTextNode (labelText));
    label.appendChild (triggerBox);
    this.elem = label;
}

ColorInput.prototype.set = function (color, hue, saturation, value) {

};

ColorInput.prototype.enable = function (isEnabled) {

};

module.exports = ColorInput;
